import { DEF_COLOR, MAIN_COLOR, SUB_COLOR, TRANSPARENT } from '../../../containers/constants'
import { mainStyle } from '../../../containers/styles'

export const timerStyle = {
    ...mainStyle,
    alignItems: 'center',
    alignContent: 'center',
    justifyContent: 'center',    
    height: '400',
    width: '100%',
    background: TRANSPARENT,
}